name: Laravel CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  laravel-tests:
    runs-on: ubuntu-latest

    services:
      mysql:
        image: mysql:8.0
        ports:
          - 3306:3306
        env:
          MYSQL_ROOT_PASSWORD: hp12c44
          MYSQL_DATABASE: farmaco_check
          MYSQL_USER: root
          MYSQL_PASSWORD: hp12c44
        options: >-
          --health-cmd="mysqladmin ping --silent"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=3

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: 8.3.14
        extensions: mysql, pdo_mysql, mbstring, openssl, pdo_mysql, xml, ctype, tokenizer, zip
        tools: composer, pest, phpunit
        coverage: none

    - name: Install dependencies
      run: |
          cd farmaco-check-app
          composer install --no-progress --prefer-dist --optimize-autoloader

    - name: Set up environment
      run: |
          cp farmaco-check-app/.env.example farmaco-check-app/.env

    - name: Generate application key
      run: |
          cd farmaco-check-app
          php artisan key:generate

    - name: Run migrations
      env:
        DB_CONNECTION: mysql
        DB_DATABASE: farmaco_check
        DB_USERNAME: root
        DB_PASSWORD: hp12c44
      run: |
          cd farmaco-check-app
          php artisan migrate

    - name: Run tests
      run: |
          cd farmaco-check-app
          vendor/bin/pest
